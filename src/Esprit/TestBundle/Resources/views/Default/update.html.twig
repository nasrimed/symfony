
{% extends 'EspritTestBundle::template.html.twig' %}

{% block body %}
    <div class="row-fluid ">
        <div class="box pinkDark span12">
            <div class="box-header" data-original-title>
                <h2><i class="halflings-icon white wrench"></i>
                    <span class="break">
                           </span>Modification Type Marque</h2>
                <div class="box-icon">
                    <a href="#" class="btn-minimize"><i class="halflings-icon chevron-up"></i></a>
                </div>
            </div>
            <div class="box-content">
                <div class="row-fluid">

                <div class="clearfix"><br> </div>
                <div class="row-fluid">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}

                    <input type="hidden" name="_target_path" value="{{ app.request.headers.get('referer') }}" />

                    <input type="hidden" name="_target_path" value="{{ app.request.headers.get('referer') }}" />
                    <script src="{{ asset('js/validateFields.js') }}"></script>
                    <table>

                    </table>
                    <br><br>
                    <input class="btn btn-info btn-validate" type="submit" value="update" />


                    {{ form_end(form) }} </div>
            </div>
        </div>
    </div>

{% endblock %}
